import PlaceholderPage from '@/components/PlaceholderPage';
export default function Page() {
  return <PlaceholderPage title="Agent Console" description="What the writing agent can retrieve, per client" />;
}
